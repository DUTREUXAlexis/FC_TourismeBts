{% extends 'base.html.twig' %}
{% block body  %}
    <h1>Connexion !</h1>
    <form action="{{ path('app_security_login') }}" method="post">
        <div class="form-group">
            <input class="form-control" placeholder="Adresse mail ..." required name="_username" type="text">
        </div>
        <div class="form-group">
            <input class="form-control" placeholder="Mot de passe ..." required name="_password" type="password">
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-success">connexion</button>
        </div>
    </form>
{% endblock %}